
@{
    ViewBag.Title = "Sales";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Sales</h2>
<table class="table table-striped table-sm table-bordered shadow-sm">
    <thead>
        <tr>
            <th>Id</th>
            <th>NumberSale</th>
            <th>UserPhone</th>
            <th>UserEmail</th>
            <th>DateSale</th>
            <th>Summa</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in ViewBag.Sales)
        {
            <tr>
                <td>@item.SaleId</td>
                <td>@item.NumberSale</td>
                <td>@item.UserPhone</td>
                <td>@item.UserEmail</td>
                <td>@item.DateSale</td>
                <td>@item.Summa</td>
            </tr>
        }
    </tbody>
</table>

<nav aria-label="">
    <ul class="pagination justify-content-end">
        <li class="page-item @(ViewBag.Id == 1 ? "disabled" : "")">
            <a class="page-link" href="~/Home/Sales/@(ViewBag.Id - 1)" tabindex="-1" aria-disabled="true">Previous</a>
        </li>

        @for (int i = 0; i < ViewBag.PageCount; i++)
        {
            <li class="page-item @((i + 1) == ViewBag.Id ? "active" : "")">
                <a class="page-link" href="~/Home/Sales/@(i + 1)">@(i + 1)</a>
            </li>
        }

        <li class="page-item @(ViewBag.Id == ViewBag.PageCount ? "disabled" : "")">
            <a class="page-link" href="~/Home/Sales/@(ViewBag.Id + 1)">Next</a>
        </li>
    </ul>
</nav>
