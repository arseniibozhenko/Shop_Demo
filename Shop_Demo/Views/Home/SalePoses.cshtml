
@{
    ViewBag.Title = "SalePoses";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>SalePoses</h2>
<table class="table table-striped table-sm table-bordered shadow-sm">
    <thead>
        <tr>
            <th>Id</th>
            <th>SaleId</th>
            <th>GoodId</th>
            <th>Category</th>
            <th>CountGood</th>
            <th>Summa</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in ViewBag.SalePoses)
        {
            <tr>
                <td>@item.SalePosId</td>
                <td>@item.SaleId</td>
                <td>@item.GoodId</td>
                <td>@item.CountGood</td>
                <td>@item.Summa</td>
            </tr>
        }
    </tbody>
</table>

<nav aria-label="">
    <ul class="pagination justify-content-end">
        <li class="page-item @(ViewBag.Id == 1 ? "disabled" : "")">
            <a class="page-link" href="~/Home/SalePoses/@(ViewBag.Id - 1)" tabindex="-1" aria-disabled="true">Previous</a>
        </li>

        @for (int i = 0; i < ViewBag.PageCount; i++)
        {
            <li class="page-item @((i + 1) == ViewBag.Id ? "active" : "")">
                <a class="page-link" href="~/Home/SalePoses/@(i + 1)">@(i + 1)</a>
            </li>
        }

        <li class="page-item @(ViewBag.Id == ViewBag.PageCount ? "disabled" : "")">
            <a class="page-link" href="~/Home/SalePoses/@(ViewBag.Id + 1)">Next</a>
        </li>
    </ul>
</nav>
